function e(e,t){if(e?.status===`suspended`)return{label:`suspended`,tone:`warning`,credential:null};if(!t.length)return{label:`no pass`,tone:`neutral`,credential:null};let n=[...t].sort((e,t)=>new Date(t.created||0).getTime()-new Date(e.created||0).getTime())[0];if(n.status===`revoked`)return{label:`revoked`,tone:`neutral`,credential:n};if(n.status===`suspended`)return{label:`suspended`,tone:`warning`,credential:n};let r=Date.now(),i=n.valid_until?new Date(n.valid_until).getTime():0,a=n.valid_from?new Date(n.valid_from).getTime():0;return i&&!isNaN(i)&&i<r?{label:`expired`,tone:`warning`,credential:n}:a&&!isNaN(a)&&a>r?{label:`not yet valid`,tone:`neutral`,credential:n}:{label:`active`,tone:`success`,credential:n}}var t=[`active`,`not yet valid`,`expired`,`revoked`,`no pass`];export{e as n,t};